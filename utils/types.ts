export type MenuType = {
  href: string;
  label: string;
};

export type SearchProps = {
  buttonClassName: string;
  ariaLabelledBy: string;
  spanID: string;
};

export type Status = {
  status: string;
};
